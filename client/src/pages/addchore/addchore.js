import React, { Component } from "react";
import { Link } from "react-router-dom";
import { Col, Row, Container } from "../../components/grid";

class addChore extends Component {
    render() {
        return (  
            <div>
                <h1>Add a Chore Yo</h1>
            </div>
        );
    }
}

export default addChore;